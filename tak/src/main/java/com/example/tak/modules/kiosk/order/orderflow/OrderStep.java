package com.example.tak.modules.kiosk.order.orderflow;

public enum OrderStep {
    SELECT_MENU,
    SELECT_TEMPERATURE,
    SELECT_SIZE,
    ASK_DETAIL_OPTION_YN,
    SELECT_DETAIL_OPTIONS,
    COMPLETE
}
